import { View } from "react-native";
import { Text } from "react-native-paper";

const TeamsScreen = () => {
  return (
    <View>
      <Text>Teams</Text>
    </View>
  );
};

export default TeamsScreen;
